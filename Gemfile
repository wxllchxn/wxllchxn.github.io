# Frozen_string_literal: true

source "https://rubygems.org"

# github-pages → nokogiri 1.16+ needs Ruby >= 3.0 (system Ruby on macOS is often 2.6).
ruby file: ".ruby-version"

gem "github-pages", "~> 232", group: :jekyll_plugins
